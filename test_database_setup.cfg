[QUADMIN_DB]
POSTGRES_USER=test
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=quadmin_db

[db]
default=postgresql+psycopg2://test:password@localhost:5432/quadmin_db

[metadata]
name = local_db_scripts
version = 0.1.0

[options]
packages = db_scripts
